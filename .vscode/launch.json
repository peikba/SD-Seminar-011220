{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Localhost",
            "request": "launch",
            "type": "al",
            "environmentType": "OnPrem",
            "server": "http://localhost",
            "serverInstance": "BC170",
            "authentication": "Windows",
            "breakOnError": true,
            "launchBrowser": true,
            "enableLongRunningSqlStatements": true,
            "enableSqlInformationDebugger": true,
            "tenant": "default",
            "schemaUpdateMode": "ForceSync" 
            // Keeps all data in the extension but schema changes deleted data in the field
            //"schemaUpdateMode": "Recreate" // Deletes all data in the extension
            //"schemaUpdateMode": "Synchronize" // Keeps all data in the extension but no schema changes
        }
    ]
}